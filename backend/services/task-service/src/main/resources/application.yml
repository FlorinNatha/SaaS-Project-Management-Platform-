server:
  port: 8084

spring:
  application:
    name: task-service
  datasource:
    url: jdbc:postgresql://localhost:5432/saas_task
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
